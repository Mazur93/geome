#![warn(missing_docs)]
/// Point structure 
pub mod point;
pub mod line;
/* possibility to make all geometry types one datatype for functions 
pub enum Geometry {
    /// A point geometry
    Point(point::Point),
    /// A line geometry
    Line(line::Line),
} */