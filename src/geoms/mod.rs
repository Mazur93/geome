/// Line struct
pub mod line;
/// Point struct
pub mod point;
/* possibility to make all geometry types one datatype for functions
pub enum Geometry {
    /// A point geometry
    Point(point::Point),
    /// A line geometry
    Line(line::Line),
} */
